# configure for _*sim case
# Note: this tb uses sys_tst_sram_s3.vbom in local directory
#       (not in .. as usual) to allow a tb specific configure !!!
# configure
s3board_aif = sys_tst_sram_s3_ssim.vhd
# design
tb_tst_sram_s3.vbom
@top:tb_tst_sram_s3
